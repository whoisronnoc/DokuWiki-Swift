# Uncomment the next line to define a global platform for your project
platform :ios, '11.0'

target 'DokuWiki' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for DokuWiki
  pod 'SWXMLHash', '~> 4.7.0'

  target 'DokuWikiTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
